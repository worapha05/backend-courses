# Lab Solution — Product Catalog API

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

ได้ JWT จาก `POST /api/token/` แล้วเรียก `/api/products/`
