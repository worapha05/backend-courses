# Example 03 — APIView / Generic / ViewSets

| Path               | แบบ                   |
| ------------------ | --------------------- |
| `/api/categories/` | APIView               |
| `/api/items/`      | Generic ListCreate    |
| `/api/items-vs/`   | ModelViewSet + Router |

```bash
pip install -r requirements.txt && python manage.py migrate && python manage.py runserver
```
