# Example 02 — ORM Queries

สาธิต `filter`, `exclude`, `Q`, และ `annotate`

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py seed_books
python manage.py runserver
```

เปิด http://127.0.0.1:8000/
