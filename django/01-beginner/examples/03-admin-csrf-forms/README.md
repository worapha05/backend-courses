# Example 03 — Admin + CSRF Forms

```bash
pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

ลอง submit form ที่ `/` แล้วตรวจใน `/admin/contact/inquiry/`
