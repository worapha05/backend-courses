# SQLite สำหรับเรียนเร็วโดยไม่ต้องเปิด Docker
DATABASE_URL="file:./dev.db"

# Production / Lab จริง — ใช้ PostgreSQL:
# DATABASE_URL="postgresql://postgres:secret@localhost:5432/nest_bootcamp?schema=public"
