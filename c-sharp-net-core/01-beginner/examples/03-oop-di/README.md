# Example 03 — OOP, Dependency Injection & Exception Pipeline

รัน:

```bash
dotnet run
```

หัวข้อที่สาธิต:

- Interface vs Abstract base (Template Method)
- `Host` + DI lifetimes
- Exception types ที่ map ได้แบบ middleware
