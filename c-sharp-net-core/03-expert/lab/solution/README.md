# Lab Solution — Orders Desk

```bash
dotnet run --project src/Orders.Api
```

Users (password `Passw0rd!`):

- `admin@example.com` (Admin)
- `manager@example.com` (Manager + orders:export)
- `viewer@example.com` (Viewer)
